/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.unsplash.com', 'plus.unsplash.com', 'unsplash.com', 'source.unsplash.com', 'assets.aceternity.com']
    }
};

export default nextConfig;